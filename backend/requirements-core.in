fastapi>=0.109.0
starlette<1.0
uvicorn[standard]>=0.27.0
uvloop>=0.19.0; sys_platform != "win32"
# onnxruntime: Python 3.12 stays on the existing tested pins. Python 3.13 uses
# current platform wheels. macOS pins 1.23.2, the newest release with both
# x86_64 and arm64 cp313 wheels; newer releases are arm64-only.
onnxruntime==1.25.0; sys_platform == "linux" and python_version < "3.13"
onnxruntime>=1.20.0; sys_platform == "linux" and python_version >= "3.13"
onnxruntime==1.19.2; sys_platform == "darwin" and python_version < "3.13"
onnxruntime==1.23.2; sys_platform == "darwin" and python_version >= "3.13"
onnxruntime==1.20.1; sys_platform == "win32" and python_version < "3.13"
onnxruntime>=1.20.0; sys_platform == "win32" and python_version >= "3.13"
# Security floor: Pillow 12.3.0 fixes multiple untrusted-image parsing issues.
Pillow>=12.3.0
# Security floor: setuptools 83.0.0 fixes GHSA-h35f-9h28-mq5c, a Unicode-normalization exclusion bypass in macOS source distributions.
setuptools>=83.0.0
# numpy: 3.12 stays on the 1.x ABI (matches existing onnxruntime/SAM3 binary
# builds, see ADR-AI-20260524 in docs/AI_DECISION_LOG.md). 3.13 has no cp313
# wheel for numpy 1.26.4 (last 1.x release), so the resolver picks 2.x.
# This produces DIFFERENT locked versions per Python (run.sh / run.bat
# selects the matching .txt at install time).
numpy>=1.24.0,<2.0.0; python_version < "3.13"
numpy>=2.1.0,<3.0.0; python_version >= "3.13"
huggingface-hub>=0.20.0
aiofiles>=23.0.0
python-multipart>=0.0.31
websocket-client>=1.8.0,<2.0
cffi>=1.14; sys_platform == "win32" and platform_python_implementation != "PyPy"
sniffio>=1.3.0
sortedcontainers>=2.4.0
# socksio: SOCKS proxy backend for httpx (httpx[socks] extra), used by the VLM
# captioner's optional SOCKS proxy field. Tiny dependency-free pure-Python lib;
# kept in core so a configured SOCKS proxy works on the lightweight install too.
socksio>=1.0.0
slowapi>=0.1.9
psutil>=5.9.0
send2trash>=1.8.3
