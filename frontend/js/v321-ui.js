/**
 * SD Image Sorter - v3.2.1 UI integration (decomposed shim)
 * The V321Integration god-object now lives in frontend/js/v321/*.js - an
 * Object.assign mixin family over the base object declared in
 * v321/base.js (each file's header records its pre-cut line range).
 * This file stays as a real, servable script because index.html and the
 * release packages reference /static/js/v321-ui.js.
 */
