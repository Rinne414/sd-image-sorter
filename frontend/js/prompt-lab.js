/**
 * SD Image Sorter - Prompt Lab (decomposed shim)
 * The PromptLab god-object now lives in frontend/js/prompt-lab/*.js - an
 * Object.assign mixin family over the base object declared in
 * prompt-lab/base.js (each file header records its pre-cut line range).
 * This file stays as a real, servable script because index.html and the
 * release packages reference /static/js/prompt-lab.js.
 */
