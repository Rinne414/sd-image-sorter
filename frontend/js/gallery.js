/**
 * SD Image Sorter - Gallery Module (decomposed shim)
 * The gallery god-object now lives in frontend/js/gallery/*.js — an
 * Object.assign mixin family over the base object declared in
 * gallery/base.js (each file's header records its pre-cut line range).
 * This file stays as a real, servable script because the index.html
 * ordering contract and the release QA gate reference /static/js/gallery.js.
 */
